build/
dist/
dev-dist/
.env
node_modules/
.DS_STORE
.idea
*.log
research/.cache/
playwright-report/
test-results/

# 오라클 대조용 로컬 캐시 (lol.ps / poro.gg 응답)

# LLM 평가 산출물 (답변 + 프롬프트 전문, 로컬 기록용)

# 과거 패치 소급 조회 캐시 (DDragon / CDragon 응답)
research/.patch-cache/

# ONNX Runtime WebGPU 런타임 (빌드 시 node_modules 에서 복사)
public/ort/

# 규칙 번역 캐시 (llm:translate-rules)
research/.rule-translations.json


# 지식 카드 번역 작업 파일 (llm:tr-knowledge --export 로 재생성)
research/knowledge-translation/

# CI 가 만드는 지식 계층 점검 글. 저장소에 남길 것이 아니다.
knowledge-drift-report.md

# 로컬 작업 자리(시안·워크트리). 푸시하지 않는다.
.claude/
